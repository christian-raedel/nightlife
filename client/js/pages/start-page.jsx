/** @jsx React.DOM */

(function () {
    'use strict';

    var React = require('react');

    var StartPage = React.createClass({
        render: function () {
            return (
                <h1>StartPage</h1>
            );
        }
    });

    module.exports = StartPage;
}());
