/** @jsx React.DOM */

(function () {
    'use strict';

    var React = require('react');

    var HomePage = React.createClass({
        render: function () {
            return (
                <h1>HomePage</h1>
            );
        }
    });

    module.exports = HomePage;
}());
